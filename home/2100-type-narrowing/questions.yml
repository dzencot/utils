---
questions:
  question-1:
    type: answers
    question: Что такое Narrowing в TypeScript?
    answers:
      wrong:
      - Это процесс преобразования типов данных в широкие типы
      - Это объединение всех механизмов работы с типами
      correct:
      - Это процесс перевода конкретного типа данных в более узкий тип

  question-2:
    type: answers
    question: Выберите наиболее узкий тип
    answers:
      wrong:
      - |
        `number | string`
      - |
        `any`
      correct:
      - |
        `number`

  question-3:
    type: filling
    question: |
      Дополните код:
    text: |

      ```javascript
        function format(value: number | boolean) {
          if (typeof value === 'number') {
            return value.toFixed(2);
          }
          if (typeof value === '%{type}') {
            return value ? '01' : '00';
          }
        }
      ```

    answers:
      type: boolean
